<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NoTitleTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CommonTextLabel">
        <item name="android:textColor">@color/common_txt_label</item>
        <item name="android:textSize">@dimen/common_txt_label</item>
    </style>

    <style name="CommonTextValue">
        <item name="android:textColor">@color/common_txt_value</item>
        <item name="android:textSize">@dimen/common_txt_value</item>
    </style>
    <style name="CommonEditText">
        <item name="android:textColor">@color/common_txt_value</item>
        <item name="android:textSize">@dimen/common_txt_value</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="CommonLine">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">@color/common_line</item>
    </style>

</resources>
